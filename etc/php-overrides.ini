[PHP]
; Profile pictures validate up to 5 MB; keep generous headroom for posts.
upload_max_filesize = 24M
post_max_size = 24M

memory_limit = 256M

; Files are baked into the image, so no timestamp checks.
opcache.enable = 1
opcache.jit = disable
opcache.validate_timestamps = 0
